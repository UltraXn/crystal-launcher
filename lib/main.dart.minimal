import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      backgroundColor: Colors.red,
      body: Center(child: Text("MINIMAL START", style: TextStyle(color: Colors.white, fontSize: 32))),
    ),
  ));
}
